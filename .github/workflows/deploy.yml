name: Deploy Auth Service

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy on server
        run: |
          cd /srv/auth-service/annora-auth
          git pull
          docker compose up -d --build
